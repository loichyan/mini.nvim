package main

func myfunc()       {}
func myfunc2(a int) {}

func main() {
	// Press <CR> to accept the selection
	// But myfunc() will follow a newline, since snippet not expanded

	// While myfunc2() will actually not follow a neline
}
